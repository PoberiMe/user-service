services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: users
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - ./data/user-service:/var/lib/postgresql/data

volumes:
  postgres_data: